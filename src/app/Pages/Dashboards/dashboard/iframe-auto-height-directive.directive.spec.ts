import { IframeAutoHeightDirectiveDirective } from './iframe-auto-height-directive.directive';

describe('IframeAutoHeightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeAutoHeightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
